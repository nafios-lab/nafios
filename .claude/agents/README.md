# `.claude/agents/`

Agent definitions for Claude Code. Each `.md` file here defines a specialized agent
with its own system prompt and tool access.

**What belongs here:** purpose-built agents — e.g. a code-review agent, a
package-scaffold agent, a spec-writer agent.

**Owning ticket:** D3 — agent definitions will be authored there. This directory
is scaffolded by D1 as a placeholder.
