{
  "name": "@nafios/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run sync && npx vitepress dev content",
    "build": "bun run sync && npx vitepress build content",
    "preview": "npx vitepress preview content",
    "sync": "bun ./scripts/sync-docs.ts",
    "typecheck": "echo 'no typecheck needed for docs'",
    "test": "echo 'no tests for docs'"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
