[test]
preload = ["./tests/setup.ts"]

# apps/web is deprecated in favour of apps/nafios-web (the Vite SPA cutover).
# It is intentionally NOT coverage-gated: without a coverage-threshold key it is
# excluded from the `test:coverage:summary` roll-up (which discovers gated
# packages by that key — see tooling/scripts/coverage-summary.ts) and from the
# per-package gate enforced under `bun run test:coverage`. Its tests still run
# under plain `bun test`. See adr/0020-test-coverage-scoping-and-gate.md.
